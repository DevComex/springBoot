<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.gyyx.action.dao.NoviceCardMapper">

	<select id="getTypeId" parameterType="String" resultType="Integer">
		SELECT
		type_id FROM novice_card WHERE card_no=#{cardNo}
	</select>
	
</mapper>
